# PR-17 — Governed liquidity-structure challenger

## Status

Local implementation and verification are complete. Independent spec and
whole-branch reviews are both Ready with 0 Critical / 0 Important / 0 Minor.
The change remains shadow-only: no push, deployment, remote database access,
production write, policy seed, or Champion promotion was performed.

## Implemented

- Frozen `LIQUIDITY_STRUCTURE_CHALLENGER_V1` before implementation. The raw
  artifact SHA-256 is `946b95679e2bbacb618251969ebb7967d8a82541d277c72297b6b0a5023cbfa0`;
  the canonical digest is
  `b9560fe595969a7f6f8420d48cdaf8f2cfd3ad45f616974469d59115ea234c38`.
- Added prior-only TGA shock/RRP Type-7 q20/q50 buffer logic and the frozen
  policy-aware WALCL matrix without changing Champion scoring.
- Added migration 0011: an empty append-only policy-regime ledger with
  database-generated non-backdatable knowledge time, validated source/date
  fields, revision lineage, immutable rows, strict as-of visibility, half-open
  effective ranges, and overlap fail-closed behavior.
- Added strict PIT liquidity inputs that share the event evaluator's exact
  database-resolved cutoff, honor visible release-calendar overrides, and choose
  the latest visible vintage before fetch-time tie breakers.
- Added exact eight-positive-factor equal/current/50-50 benchmarks and four
  Credit/Funding ablation arms. Each arm has its own sequential hysteresis pass,
  explicit challenger/digest/arm identity, and one shared 4/8/13-week outcome
  build.
- Added complete-cohort gates for governed provenance, factor coverage,
  executions, superseded/unexecuted signals, primary 13-week IC/tail maturity,
  and finite strategy/Beta-matched Sharpe, delta, and maximum drawdown.
- Added hard request limits: 600 signals and 4,000 rows each for prices, VIX,
  and cash rates. Excess work returns typed `DATA_INCOMPLETE` before formal
  validation.
- Added the versioned read-only endpoint and safely escaped Shadow dashboard
  card. Current evidence is explicitly `RETROSPECTIVE_PIT_EVENT_TIME` with
  `NOT_ESTABLISHED_NO_UNSEEN_HOLDOUT`; it is not presented as OOS.

## Files

The implementation range `74620fd`–`9415a98` changes 25 files (1,599 additions,
18 deletions): core logic in `src/liquidity-structure-challenger.ts`, PIT/ledger
queries in `src/db.ts`, endpoint wiring in `src/worker.ts`, migration 0011,
dashboard assets, protocol/research/governance documentation, deployment schema
guards, and focused database/core/API/UI tests.

## Verification

- `npm test`: 60 files / 800 tests passed.
- `npm run typecheck`: passed (`tsc --noEmit`).
- `npm run lint`: passed with zero warnings.
- `npm run test:correctness`: 5 files / 79 tests passed.
- `npm run test:no-lookahead`: 4 files / 42 tests passed.
- `npm run test:rebuild-consistency`: 1 file / 5 tests passed, including full
  versus incremental same-date consistency.
- `npm run migrate:verify`: migrations 0001–0011 applied from empty; second run
  reported no migrations to apply.
- `npm run restore:drill`: 11/11 migrations restored with identical content hash
  `f2e922ae9e209cc75f98f2d4e02ca3b792d4a6225e7f0b7e7d74b90b7dc5bc6a`.
- `npm run deploy:dry`: staging bundle dry-run passed and exited without deploy.
- `git diff --check`: passed.

## Independent review

- Spec review: Ready, 0 Critical / 0 Important / 0 Minor.
- Whole-branch code review: Ready, 0 Critical / 0 Important / 0 Minor.
- Review-driven regressions cover release overrides/latest vintage selection,
  one decision cutoff, per-arm hysteresis, backdated policy knowledge,
  retrospective/OOS disclosure, bounded four-pass work, mixed cohort identity,
  same-close supersession, and null zero-volatility Sharpe metrics.

## Known limitations

- Migration 0011 deliberately has no policy seeds. Policy-aware output remains
  unavailable until reviewed primary-source events are appended.
- The frozen protocol has no unseen holdout boundary. ALG-08 OOS evidence remains
  pending a separately preregistered prospective holdout and future maturity.
- The endpoint is request-time shadow diagnostics with hard bounds; no production
  shadow runtime, cache, or production-scale performance test was performed.
- Historical legacy/migration-backfilled, mixed, incomplete-factor, missing-price,
  superseded/unexecuted, immature, and null-metric cohorts fail closed rather than
  borrowing samples or reporting neutral/zero values.

## Rollback

Revert the PR-17 commit range after base `52d1276`. If migration 0011 exists only
in an ephemeral local D1, rebuild that fixture. If it is ever applied to a shared
database, do not drop the table or mutate/delete append-only rows; disable readers
with an application rollback or forward migration. Champion formulas, weights,
thresholds, hysteresis, snapshots, and portfolio policy require no data rollback
because they were not changed.
